#include <stdio.h>

int main()
{
    int a;
    int b; 
    printf("enter your integere :");
    scanf("%d %d", &a , &b);

    printf("\n a + b is %d", a + b);
    printf("\n a * b is %d", a * b);
    printf("\n a / b is %d", a / b);
    printf("\n a % b is %d", a % b);
  return 0;
}